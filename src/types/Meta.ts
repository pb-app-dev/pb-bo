export interface Meta {
    totalItems: number;
    itemsPerPage: number;
    totalPages: number;
    currentItems: number;
    currentPage: number;
    hasPreviousPage: boolean;
    hasNextPage: boolean;
}
